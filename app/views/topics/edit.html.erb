<% @title = 'Editing topic" %>

<% form_for(@topic) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </p>
  <%= form_buttons( f, "Save", topics_url ) %>
<% end %>

