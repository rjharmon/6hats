<% @title = "New topic" %>

<% form_for(@topic) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </p>
  <%= form_buttons( f, "Create", topics_url ) %>
<% end %>

<%= link_to 'Back', topics_path %>
