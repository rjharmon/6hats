<h1>Editing thought</h1>

<!--% form_for(@thought, :url => { :action => 'edit', :controller => 'thoughts', :topic_id => @topic, :thought_id => @thought }) do |f| %-->
<% 
form_for( [ @topic, @thought ] ) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.label :hat_id %><br />
    <%= f.text_field :hat_id %>
  </p>
  <p>
    <%= f.label :summary %><br />
    <%= f.text_field :summary %>
  </p>
  <p>
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </p>
  <p>
    <%= f.submit "Update" %>
  </p>
<% end %>

<%= link_to 'Back', [ @topic ] %>
